local: true

sources_config:
  ids: [1]
  default:
    inf_frame: 2
    conf_threshold: 0.01
    nms_iou_threshold: 0.5

elastic_config:
  url: http://localhost:9200
  index_name: embeddings-live
  
triton_config:
  url: http://localhost:8001

inference_config:
  task: ObjectDetection
  instances: 77
  models:
    YOLO:
      name: yolov9-t
      precision: FP16
      input_name: images
      input_shape: [3, 640, 640]
      nms_in_triton: false
      output_name: output
      output_shape: [84, 8400]
      # CPU NMS:
      #   keep `nms_in_triton: false`
      #   keep `output_name` + `output_shape`
      #
      # Triton packed EfficientNMS:
      #   set `nms_in_triton: true`
      #   use a single packed output named `det_packed`
      #   shape is [1 + 6*K], where K = max_output_boxes in inject_nms.py
      #   for the default K=300, use [1801]
      #
      # Preferred explicit form:
      # outputs:
      #   - name: det_packed
      #     data_type: FP32
      #     shape: [1801]
      #
      # Legacy single-output form is also supported:
      # output_name: det_packed
      # output_shape: [1801]
      batch_max_size: 2
      batch_max_queue_delay: 1500
      batch_preferred_sizes: [1, 2]

    # DINO:
    #   name: dinov3-vitb16
    #   precision: FP16
    #   input_name: images
    #   input_shape: [3, 512, 512]
    #   output_name: output
    #   output_shape: [768]
    #   batch_max_size: 16
    #   batch_max_queue_delay: 1000
    #   batch_preferred_sizes: [4, 8, 16]

    # DINO_OBJECTS:
    #   name: dinov3-bboxes
    #   precision: FP16
    #   input_name: images
    #   input_shape: [3, 224, 224]
    #   output_name: output
    #   output_shape: [768]
    #   batch_max_size: 16
    #   batch_max_queue_delay: 1000
    #   batch_preferred_sizes: [4, 8, 16]
